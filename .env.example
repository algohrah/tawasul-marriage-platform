# Local development mode: use "local" for IndexedDB/localStorage fallback.
# Use "api" when running the server/API routes against Supabase.
VITE_DATABASE_PROVIDER="api"
APP_URL="http://localhost:3000"

# Tawafok Supabase project (public project URL)
VITE_SUPABASE_URL="https://lzseyykpkputgfvgnddc.supabase.co"
VITE_SUPABASE_ANON_KEY=""
NEXT_PUBLIC_SUPABASE_URL="https://lzseyykpkputgfvgnddc.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

# Server-side only — never commit a real service-role key.
SUPABASE_SERVICE_ROLE_KEY=""

# Optional production authentication
VITE_GOOGLE_CLIENT_ID=""
VITE_GOOGLE_AUTH_PROXY=""

# Never commit real secrets. Configure the public key and server key in the hosting provider.
